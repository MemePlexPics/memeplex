export enum ECallback {
  IGNORE = 'ignore',
  POST = 'post',
  KEY = 'key',
  GROUP = 'gr',
  GROUP_KEYWORD = 'gr_key',
  PAY = 'pay',
  SEARCH_NEXT_PAGE = 'search_next_page',
  LATEST = 'latest',
}
