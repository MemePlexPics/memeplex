{
  "name": "memeplex",
  "type": "module",
  "version": "1.0.0",
  "description": "MemePlex is a search engine for memes with a focus on Russian underground Telegram / это поисковая система для мемов нижнего Телеграма",
  "main": "services/memesearch.js",
  "scripts": {
    "eslint": "eslint src/ services/",
    "init": "npm install && mkdir -p logs data/{elastic,grafana,loki,mysql,promtail,rabbitmq} && cp -n .env.example .env && cp -n .mysql.env.example .mysql.env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "klntsky",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^8.11.0",
    "amqplib": "^0.10.3",
    "axios": "^1.6.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "form-data": "^4.0.0",
    "imghash": "^0.0.9",
    "mysql2": "^3.7.0",
    "ocr-space-api-wrapper": "^2.1.3",
    "ramda": "^0.29.1",
    "socks-proxy-agent": "^8.0.2",
    "telegraf": "^4.15.3",
    "telegraf-ratelimit": "^2.0.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
