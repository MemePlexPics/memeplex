import stylex from '@stylexjs/stylex'

export const s = stylex.create({
    addChannelForm: {
      maxWidth: '800px',
    },
    inputContainer: {
      display: 'flex',
      flexDirection: 'row',
      gap: '4px',
    },
})
